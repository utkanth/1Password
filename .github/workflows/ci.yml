name: CI with 1Password Secrets Automation for Interview

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # allows manual trigger for demo

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt update && sudo apt install -y unzip curl

      - name: Install 1Password CLI (direct download)
        run: |
          echo "🔧 Downloading and installing 1Password CLI..."
          curl -sSL https://cache.agilebits.com/dist/1P/op2/pkg/v2.30.3/op_linux_amd64_v2.30.3.zip -o op.zip
          unzip -q op.zip
          sudo mv op /usr/local/bin/
          op --version
          echo "✅ 1Password CLI installed successfully."

      - name: Sign in, fetch, create, and update existing API_KEY (Fixing Date Fields)
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
        run: |
          # Define item names and vault name
          ITEM_TO_UPDATE="API_KEY"
          VAULT="1password PoC"
          
          # --- 1. READ/FETCH (Initial Read) ---
          API_KEY_CURRENT_VALUE=$(op read "op://${VAULT}/${ITEM_TO_UPDATE}/username")
          
          # ----------------------------------------------------------------------
          # --- 2. ROTATE/UPDATE EXISTING API_KEY (JSON Method - FIXED) ---
          # ----------------------------------------------------------------------
          TODAYS_DATE=$(date +%Y-%m-%d)
          NEW_API_KEY_VALUE="${API_KEY_CURRENT_VALUE%_*}_${TODAYS_DATE}"
          
          echo "✏️ Updating the '${ITEM_TO_UPDATE}' to append date: ${TODAYS_DATE}..."

          # a) Fetch the current item's JSON
          CURRENT_JSON=$(op item get "${ITEM_TO_UPDATE}" --vault "${VAULT}" --format json)
          
          # b) Use jq to update ONLY the username field's value. 
          #    This logic ensures all other fields (including valid from/expires) 
          #    are passed through unmodified, retaining their original values.
          UPDATED_JSON=$(echo "${CURRENT_JSON}" | jq \
            --arg new_val "$NEW_API_KEY_VALUE" \
            '.fields |= map(
              if .label == "username" then 
                # This only updates the VALUE of the username field
                .value = $new_val 
              else 
                # This passes ALL other fields (including date fields) through without modification
                . 
              end
            )')

          # c) Pipe the modified JSON back to the edit command
          echo "${UPDATED_JSON}" | op item edit "${ITEM_TO_UPDATE}" --vault "${VAULT}" -
            
          echo "✅ Secret '${ITEM_TO_UPDATE}' updated successfully."

      - name: Manual trigger confirmation
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          echo "🧑‍💻 Workflow triggered manually via the 'Run workflow' button."
          echo "Branch: ${{ github.ref }}"
          echo "Commit: ${{ github.sha }}"
